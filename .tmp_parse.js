const { parsePeg } = require('..packagesclisrcassemblerparserpegAdapter'); const fs = require('fs'); const path = require('path'); const src = fs.readFileSync(path.join(process.cwd(), 'examples/p1-d/fixture-p1d.asm'), 'utf8'); const ctx = { currentPos: { file: path.join(process.cwd(), 'examples/p1-d/fixture-p1d.asm'), line: 0, column: 0, phase: 'parse' }, options: {}, symbols: new Map(), externs: new Map() }; const nodes = parsePeg(ctx, src); console.log(nodes.filter(n => n.kind === 'pseudo' ; n.op === 'END')); console.log(nodes.filter(n => n.kind === 'label' ; n.name === 'START'));