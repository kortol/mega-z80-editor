import { CpuState } from "./core";
type CpmEnv = {
    read8: (addr: number) => number;
    write8: (addr: number, value: number) => void;
    getDma: () => number;
    setDma: (addr: number) => void;
    output: (text: string) => void;
    rootDir: string;
    trace?: boolean;
    interactive?: () => boolean;
    readConsoleChar?: (blocking: boolean) => number | undefined;
    hasConsoleChar?: () => boolean;
    readConsoleLine?: (maxLen: number) => string;
};
export declare class CpmBdos {
    private env;
    private handles;
    private searchResults;
    private searchIndex;
    constructor(env: CpmEnv);
    setRootDir(rootDir: string): void;
    setTrace(trace: boolean): void;
    private traceLog;
    private traceExtra;
    handle(fn: number, state: CpuState): string | undefined;
    private writeDollarString;
    private readBufferedConsole;
    private fcbAddr;
    private decodeFcbChar;
    private readFcb;
    private buildHostName;
    private listMatching;
    private patternToRegex;
    private writeDirEntry;
    private openFile;
    private closeFile;
    private makeFile;
    private renameFile;
    private deleteFiles;
    private searchFirst;
    private searchNext;
    private getHandleFor;
    private readSequential;
    private writeSequential;
    private readRandom;
    private writeRandom;
    private computeFileSize;
    private setRandomRecord;
}
export {};
