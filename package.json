{
  "name": "mega-z80-editor",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm store prune && rimraf node_modules */*/node_modules",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r --if-present run test"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
